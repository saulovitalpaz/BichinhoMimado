// src/components/financial/CashierModule.tsx
import { Wallet, User, Dog, CheckCircle } from 'lucide-react';

const CashierModule = () => {
  // Simulação de dados vindo do "Atendimento Médico"
  const pendingBills = [
    {
      id: "inv-001",
      owner: "João Silva",
      pet: "Bento (Golden)",
      services: [
        { desc: "Consulta Geral", price: 150.00 },
        { desc: "Vacina V10", price: 85.00 }
      ],
      total: 235.00,
      timestamp: "10:30"
    }
  ];

  return (
    <div className="grid grid-cols-1 md:grid-cols-3 gap-6 p-6 bg-gray-50">
      {/* Lista de Clientes Saindo da Consulta */}
      <div className="md:col-span-2 space-y-4">
        <h2 className="text-2xl font-bold text-gray-800 flex items-center gap-2">
          <Wallet className="text-green-600" /> Contas a Receber (Caixa)
        </h2>
        
        {pendingBills.map(bill => (
          <div key={bill.id} className="bg-white border-l-4 border-orange-400 p-4 rounded-lg shadow-sm hover:shadow-md transition-all">
            <div className="flex justify-between items-start">
              <div>
                <div className="flex items-center gap-2 text-sm text-gray-500">
                  <User size={14} /> <span className="font-semibold text-gray-700">{bill.owner}</span>
                  <Dog size={14} className="ml-2" /> <span>{bill.pet}</span>
                </div>
                <div className="mt-2 space-y-1">
                  {bill.services.map((s, i) => (
                    <div key={i} className="text-sm text-gray-600 flex justify-between w-64">
                      <span>{s.desc}</span>
                      <span className="font-mono">R$ {s.price.toFixed(2)}</span>
                    </div>
                  ))}
                </div>
              </div>
              <div className="text-right">
                <p className="text-xs text-gray-400">Finalizado às {bill.timestamp}</p>
                <p className="text-xl font-bold text-green-700">R$ {bill.total.toFixed(2)}</p>
                <button className="mt-2 bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700 flex items-center gap-2 transition-colors">
                  <CheckCircle size={18} /> Baixar Pagamento
                </button>
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* Resumo Financeiro do Dia (Sidebar) */}
      <div className="bg-white p-6 rounded-xl shadow-sm border border-gray-100 h-fit">
        <h3 className="font-bold text-gray-700 mb-4">Resumo do Turno</h3>
        <div className="space-y-4">
          <div className="p-3 bg-green-50 rounded-lg">
            <p className="text-sm text-green-600">Total Recebido</p>
            <p className="text-2xl font-bold text-green-700">R$ 1.450,00</p>
          </div>
          <div className="p-3 bg-orange-50 rounded-lg">
            <p className="text-sm text-orange-600">Pendente (Em atendimento)</p>
            <p className="text-2xl font-bold text-orange-700">R$ {pendingBills[0].total.toFixed(2)}</p>
          </div>
        </div>
      </div>
    </div>
  );
};