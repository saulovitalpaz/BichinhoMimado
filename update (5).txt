/web
  /src
    /assets (imagens, ícones)
    /components (botões, cards, inputs reutilizáveis)
    /styles (variáveis globais, temas)
    /modules
      /dashboard
      /medical (prontuários, agenda)
      /financial (fluxo de caixa, segurança)
    /utils (validadores de CPF/CNPJ, máscaras de moeda)